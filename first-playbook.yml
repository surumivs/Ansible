---

- name: Installing and starting the nginx
  hosts: all
  become: true
  become_user: root

  tasks:
  - name: install nginx
    apt:
     name: nginx
     state: present
  - name: start nginx
    service:
     name: nginx
     state: started
